name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: build project with debug flags
      run: |
        cmake -DCMAKE_BUILD_TYPE=Debug
        cmake --target CppGitHubCICDByExample -- -j$(nproc)
